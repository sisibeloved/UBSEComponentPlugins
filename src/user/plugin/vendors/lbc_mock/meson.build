libssu_plugin_lbc_mock = shared_library(
  'ssu_plugin_lbc_mock',
  sources: ['ssu_plugin_lbc_mock.cpp'],
  include_directories: inc,
  dependencies: [ssu_headers_dep],
  install: true,
  install_dir: join_paths(get_option('libdir'), 'ubs-engine', 'ssu'),
)
